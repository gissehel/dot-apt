function ensure-packages-removed {
    packages=$(echo $@)
    packages_to_remove=""
    [ $(dpkg -l ${packages} 2>&1 >/dev/null | wc -l) == "0" ] && packages_to_remove=${packages}
    [ -n "${packages_to_remove}" ] && sudo apt remove -y ${packages_to_remove}
}
