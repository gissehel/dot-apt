function ensure-packages-installed {
    packages=$(echo $@)
    packages_to_install=""
    [ $(dpkg -l ${packages} 2>&1 >/dev/null | wc -l) == "0" ] || packages_to_install=${packages}
    [ -n "${packages_to_install}" ] && sudo apt update -y && sudo apt install -y ${packages_to_install}
}
